import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Forms",
  description: "Advanced form example using react-hook-form and Zod.",
};

import EditorPage from "./[section]/page";
export default EditorPage;
